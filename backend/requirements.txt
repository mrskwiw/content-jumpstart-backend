# =====================================================================
# Backend (web server) dependencies.
# Shared deps (anthropic, pydantic, python-dotenv, pytest, pytrends,
# pandas, redis, dev tools, ...) come from the canonical root file — do
# NOT re-pin them here (that caused CI ResolutionImpossible). This file
# adds only the FastAPI web-server layer on top.
# =====================================================================
-r ../requirements.txt

# FastAPI and Server
fastapi==0.115.14          # security: >=0.115 pulls starlette>=0.40 (fixes CVE-2024-47874 multipart DoS)
uvicorn[standard]==0.27.0
python-multipart==0.0.6
sse-starlette==1.8.2

# Database
sqlalchemy==2.0.25
alembic==1.13.1
psycopg2-binary==2.9.9  # PostgreSQL driver for SQLAlchemy

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# Pin bcrypt <4.2: passlib 1.7.4 (unmaintained) probes the backend by hashing a
# >72-byte test value; bcrypt >=4.2 raises "password cannot be longer than 72
# bytes" there instead of truncating, breaking ALL passlib hashing (auth password
# verify + MFA backup codes). 4.1.2 is verified working. (surfaced by CI)
bcrypt==4.1.2

# Validation
email-validator==2.1.0

# Rate Limiting (TR-004)
slowapi==0.1.9

# Async support
aiofiles==23.2.1

# HTTP clients (backend + tests)
httpx==0.26.0
requests==2.31.0

# Agent integration helpers (backend-only)
cachetools==5.3.2  # In-memory caching for query results
types-cachetools==5.3.0.7  # Type stubs for cachetools
textstat==0.7.3    # Readability scoring for voice analysis

# Background Job Queue (Phase 2 optimization)
celery==5.3.6  # Distributed task queue
flower==2.0.1  # Celery monitoring UI (optional)
