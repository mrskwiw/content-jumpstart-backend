# =====================================================================
# CANONICAL requirements — single source of truth for shared + CLI/agent
# + dev/test deps. backend/requirements.txt layers web-server deps on top
# via `-r ../requirements.txt`. Do not duplicate a pin across both files.
# =====================================================================

# Core Dependencies
anthropic==0.74.1
python-dotenv==1.0.1
pydantic==2.5.3
pydantic-settings==2.1.0

# CLI & Terminal
click==8.1.7
rich==13.7.0

# Template Processing
jinja2==3.1.6          # security: fixes CVE-2024-22195/34064/56201/56326, CVE-2025-27516
pyyaml==6.0.1

# Utilities
python-dateutil==2.8.2
datasketch==1.6.4           # MinHash/LSH for efficient similarity detection

# Phase 5: Multi-Format Output
python-docx==1.1.0      # DOCX deliverable generation
reportlab==4.2.5        # PDF brief generation (client research download)
openpyxl==3.1.0         # Excel analytics tracker
icalendar==5.0.11       # iCal posting schedule export

# Google Trends Integration
pytrends>=4.9.2         # Unofficial Google Trends API (latest stable version)
urllib3>=1.26.19,<2.0  # <2.0 for pytrends compat (deprecated method_whitelist); floor patches CVE-2024-37891/CVE-2023-45803
pandas>=1.5.0           # Required by pytrends for dataframes

# Testing
pytest==8.4.2
pytest-asyncio==1.3.0
pytest-cov==7.0.0
pytest-mock==3.12.0
pytest-timeout==2.4.0       # bound network-dependent integration tests so CI can't hang
faker==22.0.0
openapi-core==0.19.0       # OpenAPI 3.x schema validation
schemathesis==3.27.1       # Property-based API testing from OpenAPI schemas

# Development Tools — pinned to match .pre-commit-config.yaml and CI
black==25.12.0
ruff==0.14.10
mypy==1.8.0
pre-commit==3.6.0
redis==5.0.1
pyotp==2.9.0
qrcode[pil]==7.4.2

# Stripe Payments
stripe>=7.0.0
